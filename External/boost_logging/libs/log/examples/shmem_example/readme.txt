First, run the reader application.

Then, before pressing a key (on the reader application),
run the writer. 

After the writer application has run,
go to the reader application and press a key.

Then, take a look at the contents of reader/log.txt file
(just what the writer wrote!)


