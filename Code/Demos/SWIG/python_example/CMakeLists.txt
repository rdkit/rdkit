FIND_PACKAGE(PythonLibs)
INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH})

SET_SOURCE_FILES_PROPERTIES(RDKFuncs.i PROPERTIES CPLUSPLUS ON)
SET_SOURCE_FILES_PROPERTIES(RDKFuncs.i PROPERTIES SWIG_FLAGS "")

SET(CMAKE_SWIG_OUTDIR ${CMAKE_CURRENT_SOURCE_DIR} )
SWIG_ADD_MODULE(pyRDKFuncs "python" RDKFuncs.i ../RDKFuncs.cpp)
SWIG_LINK_LIBRARIES(pyRDKFuncs ${RDKit_Wrapper_Libs} ${PYTHON_LIBRARIES})

INSTALL(TARGETS _pyRDKFuncs 
              DESTINATION ${CMAKE_CURRENT_SOURCE_DIR} )

